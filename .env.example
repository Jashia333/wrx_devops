# API base URL for the Next.js frontend (used in Docker Compose).
# For local dev with backend on host: http://127.0.0.1:8000
# With Docker Compose, frontend uses: http://backend:8000
NEXT_PUBLIC_API_URL=http://backend:8000
